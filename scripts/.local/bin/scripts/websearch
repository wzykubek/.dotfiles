#!/usr/bin/env sh

# Search web with command.

SEARCH_URL="https://duckduckgo.com/?q="

case "$1" in
http?://*) xdg-open "$1" ;;
*)
    xdg-open "${SEARCH_URL}$(
        IFS=+
        echo "$*"
    )"
    ;;
esac
